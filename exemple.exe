#include <stdio.h>

int main() {
    int N, facto, cpt, i;

    puts("Entrez un entier: ");
do
{
    scanf("%d", &N);
    if (! N>0 && N % 2 == 0 && N % 10 == 0 && N % 5 == 0 )   
    puts("Entrez un entier positif , pair, divisble par 10 et multiple de 5: ");
}While
 {(! N>0 && N % 2 == 0 && N % 10 == 0 && N % 5 == 0 )

}
for (i=1; i<=n; i++) {
cpt=0;
for (j=1; j<=n; ji++) {
if  (i%j==0){
cpt++
}
}
if (cpt==2){facto=1;
for (j=1;j<=i; j++){ facto=facto*j;}
printf( "Le factoriel de %d est %d",i ,facto);
}}}


   
